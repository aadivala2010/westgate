node_modules/
.next/
out/
build/
.vercel/
.env*
!.env.example
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Source media — the mp4 is the input to scripts/frames.sh, not a build artifact.
# The generated public/frames/ WebPs ARE committed.
assets/
*.mp4

# Next.js writes these for AI agents on dev start; not part of the site.
AGENTS.md
CLAUDE.md
